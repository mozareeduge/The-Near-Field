{
  "name": "nearby-field",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/web",
    "apps/worker"
  ],
  "scripts": {
    "dev": "concurrently -k -n worker,web -c auto \"npm run dev:worker\" \"npm run dev:web\"",
    "dev:web": "npm --workspace apps/web run dev",
    "dev:worker": "npm --workspace apps/worker run dev",
    "build": "npm --workspace apps/web run build && npm --workspace apps/worker run check",
    "test": "node --experimental-strip-types --test tests/*.test.mjs",
    "qa": "bash qa-release.sh"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
