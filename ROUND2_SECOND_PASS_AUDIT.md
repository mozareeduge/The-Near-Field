# Nearby Field — Round 2 adversarial second pass

Candidate attacked: initial `NF-R2-0.2.0-A`  
Revised candidate: `NF-R2-0.2.0-B` (current)

| Location/rule | Stress scenario | Failure found | Root cause | Revision |
|---|---|---|---|---|
| Harpers Ferry fixture | multi-node path without live route provider | historical fixture remained `VERIFIED` | proof state inherited from documentary analysis | preserve legacy separately; current fixture is `RELATIONAL_UNVERIFIED` |
| Gatherer role embedding | exact skill authority | embedded role was vulnerable to silent editorial drift | prompt copied into code without fidelity test | exact role + prompt-fidelity regression |
| Synthesizer role embedding | exact skill authority | one formatting construct around the source transform was missing | template-literal simplification | restored canonical text using escaped backticks + fidelity test |
| selected map ↔ prose object | hover/focus then click/pin across surfaces | transient candidate hover did not always drive the same prose place | active and hovered meanings overloaded | separate `hoverPlace` and pinned `activePlace`; derive common displayed place |
| model JSON | valid-shaped invented IDs/offsets | schema could pass impossible identity relations | structural validation treated as epistemic validation | exact post-model identity/evidence/offset validation + one repair |
| route rendering | matrix exists but Directions fails | path could have been visually promoted too early | ordering evidence conflated with geometry evidence | solid route only after valid Directions LineString; downgrade otherwise |
| browser evidence | source checks are green | risk of reporting implementation as rendered behavior | unavailable dependency/browser harness | explicit `VERIFICATION_INCOMPLETE`; no screenshot/runtime claim |

## What Pass B can do that Pass A could not

- distinguish transient and pinned cross-media selection correctly;
- prove the static role texts have not drifted from the canonical skill;
- preserve old Harpers Ferry evidence without letting it control current route truth;
- reject syntactically valid but epistemically impossible model output;
- keep browser/live-provider gaps visible instead of laundering them through fixtures.

No blocker was found that can be repaired further without the missing canonical browser/provider execution route. Those proof gaps are therefore passed explicitly into Round 3 rather than hidden.
